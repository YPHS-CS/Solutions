#include <iostream>
#include <algorithm>
#include <cmath>
#include <vector>
using namespace std;

typedef vector<int> vi;

const int MAX_N = 3e5+2;
vi nums(MAX_N);
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    return 0;
}